// Set via IOS_DEVELOPMENT_TEAM in .env.local (see .env.example); kept out of
// the repo so the public source carries no account identifiers.
export const APPLE_DEVELOPMENT_TEAM_ID = '';
export const IOS_APP_BUNDLE_ID = 'com.improvtoolbox.app';
